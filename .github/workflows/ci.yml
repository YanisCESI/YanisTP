name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-test-package:
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Build
        run: echo "Build terminé"

      - name: Test
        run: echo "Tests réussis"

      - name: Package
        run: |
          mkdir dist
          echo "artifact ok" > dist/result.txt
